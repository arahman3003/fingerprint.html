<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Fingerprint Access</title>
<style>
  body {
    margin: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: radial-gradient(circle at center, #020202 60%, #000 100%);
    font-family: 'Lexend', sans-serif;
    color: #00eaff;
    overflow: hidden;
  }

  .fingerprint-container {
    text-align: center;
    position: relative;
  }

  .circle {
    width: 200px;
    height: 200px;
    border: 3px solid #00eaff;
    border-radius: 50%;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation: pulse 2s infinite ease-in-out;
    box-shadow: 0 0 25px #00eaff;
  }

  .fingerprint {
    width: 120px;
    height: 120px;
    cursor: pointer;
    filter: drop-shadow(0 0 20px #00eaff);
    z-index: 2;
    position: relative;
    animation: glow 2s infinite ease-in-out;
    transition: transform 0.3s ease, filter 0.3s ease;
  }

  .fingerprint:hover {
    transform: scale(1.05);
    filter: drop-shadow(0 0 35px #00ffff);
  }

  p {
    color: white;
    font-weight: bold;
    font-size: 18px;
    margin-top: 160px;
    animation: blink 2s infinite;
  }

  @keyframes glow {
    0%, 100% { filter: drop-shadow(0 0 15px #00eaff); }
    50% { filter: drop-shadow(0 0 40px #00ffff); }
  }

  @keyframes pulse {
    0% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
    100% { transform: translate(-50%, -50%) scale(1.3); opacity: 0; }
  }

  @keyframes blink {
    50% { opacity: 0.5; }
  }

  /* Scan animation */
  .scanning {
    animation: scanEffect 0.8s ease-in-out forwards;
  }

  @keyframes scanEffect {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(0.9); opacity: 0.7; }
    100% { transform: scale(1.2); opacity: 0; }
  }
</style>
</head>
<body>

<div class="fingerprint-container">
  <div class="circle"></div>
  <img src="https://i.imgur.com/pKjJ4fN.png" alt="Fingerprint" id="fingerprint" class="fingerprint">
  <p>Sentuh untuk Masuk Dashboard</p>
</div>

<script>
  const fingerprint = document.getElementById('fingerprint');
  fingerprint.addEventListener('click', () => {
    fingerprint.classList.add('scanning');
    setTimeout(() => {
      window.location.href = "https://script.google.com/macros/s/AKfycby6ITviQAhnXzyvi2F7pVPFFkk5PKf5wanIck4K4HhYaNpgVRxpZ2j4VK3FKYOdbh5KnA/exec";
    }, 900);
  });
</script>

</body>
</html>
